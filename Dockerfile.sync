# Sync server only — pair with Vercel/static frontend via VITE_WS_URL
FROM node:22-alpine
WORKDIR /app

ENV NODE_ENV=production
ENV HOST=0.0.0.0

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server

EXPOSE 10000

CMD ["node", "server/sync-server.mjs"]
